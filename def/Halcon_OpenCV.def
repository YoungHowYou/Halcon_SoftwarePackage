
remap<- CHCremap[::hv_DictHandle:]
short.german
  Laplace Filter.;
  
short.english
  几何变换重映射，对应 cv::remap。输入图/输出图/MapX/MapY 以 dict 对象传入（uint2 输入，real 映射图）；每个像素按映射表取源坐标，支持桶形/枕形畸变校正等。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  hv_DictHandle:      input_control;
  description.english: dict 句柄（输入图/输出图/MapX/MapY 以 dict 对象传入）。;
  default_type:       handle;
  multivalue:         false;
  sem_type:           handle;
  type_list:          handle;


PNGOut<-CHPNGOut[inimage:outimage::]
short.german
  Laplace Filter.;
  
short.english
  PNG 图像编码输出。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  inimage:           input_object;
  description.english: byte「乱码图」;
  sem_type:           image;
  multivalue:         optional;

parameter
  outimage:           output_object;
  description.english: 还原后的图像（8/16 位，1/3 通道）;
  sem_type:           image;
  multivalue:         optional;

PNGIn<-CHPNGIn[inimage:outimage:acceleration:]
short.german
  Laplace Filter.;
  
short.english
  PNG 图像解码输入。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  inimage:           input_object;
  description.english: 8/16 位，单/三通道;
  sem_type:           image;
  multivalue:         optional;

parameter
  outimage:           output_object;
  description.english: byte「乱码图」;
  sem_type:           image;
  multivalue:         optional;

parameter
  acceleration:       input_control;
  description.english: PNG 压缩级别;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;


add_roi<-Cadd_roi[smallimage,bigimage::sy,sx,ew,eh:]
short.german
  Add ROI to big image.;
  
short.english
  ROI 加法：大图指定矩形区域与同样大小的小图逐像素相加（A + B∩Roi），结果写回大图（原地修改）。仅支持 uint2；小图尺寸必须等于 ROI（ew×eh）。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  smallimage:         input_object;
  description.english: 小图（uint2，尺寸必须等于 ROI 的 ew×eh）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  bigimage:           input_object;
  description.english: 大图（uint2，结果原地写回）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  sy:                 input_control;
  description.english: ROI 左上角行坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  sx:                 input_control;
  description.english: ROI 左上角列坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  ew:                 input_control;
  description.english: ROI 宽度（= 小图宽度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  eh:                 input_control;
  description.english: ROI 高度（= 小图高度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;


mul_roi<-Cmul_roi[smallimage,bigimage::sy,sx,ew,eh:]
short.german
  Multiply ROI to big image.;
  
short.english
  ROI 乘法：大图 ROI 与小图逐像素相乘，结果写回大图（原地修改）。仅支持 uint2。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  smallimage:         input_object;
  description.english: 小图（uint2，尺寸必须等于 ROI 的 ew×eh）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  bigimage:           input_object;
  description.english: 大图（uint2，结果原地写回）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  sy:                 input_control;
  description.english: ROI 左上角行坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  sx:                 input_control;
  description.english: ROI 左上角列坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  ew:                 input_control;
  description.english: ROI 宽度（= 小图宽度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  eh:                 input_control;
  description.english: ROI 高度（= 小图高度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;


sub_B_roi<-Csub_B_roi[smallimage,bigimage::sy,sx,ew,eh:]
short.german
  Subtract ROI from big image (B - small).;
  
short.english
  ROI 减法（B 系）：小图减大图 ROI（B − A∩Roi），结果写回大图。仅支持 uint2。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  smallimage:         input_object;
  description.english: 小图（uint2，尺寸必须等于 ROI 的 ew×eh）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  bigimage:           input_object;
  description.english: 大图（uint2，结果原地写回）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  sy:                 input_control;
  description.english: ROI 左上角行坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  sx:                 input_control;
  description.english: ROI 左上角列坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  ew:                 input_control;
  description.english: ROI 宽度（= 小图宽度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  eh:                 input_control;
  description.english: ROI 高度（= 小图高度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;


div_B_roi<-Cdiv_B_roi[smallimage,bigimage::sy,sx,ew,eh:]
short.german
  Divide ROI from big image (B / small).;
  
short.english
  ROI 除法（B 系）：小图除以大图 ROI（整除），结果写回大图。仅支持 uint2。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  smallimage:         input_object;
  description.english: 小图（uint2，尺寸必须等于 ROI 的 ew×eh）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  bigimage:           input_object;
  description.english: 大图（uint2，结果原地写回）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  sy:                 input_control;
  description.english: ROI 左上角行坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  sx:                 input_control;
  description.english: ROI 左上角列坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  ew:                 input_control;
  description.english: ROI 宽度（= 小图宽度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  eh:                 input_control;
  description.english: ROI 高度（= 小图高度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;


div_A_roi<-Cdiv_A_roi[smallimage,bigimage::sy,sx,ew,eh:]
short.german
  Divide big image by ROI (small / B).;
  
short.english
  ROI 除法（A 系）：大图 ROI 除以小图（整除），结果写回大图。仅支持 uint2。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  smallimage:         input_object;
  description.english: 小图（uint2，尺寸必须等于 ROI 的 ew×eh）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  bigimage:           input_object;
  description.english: 大图（uint2，结果原地写回）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  sy:                 input_control;
  description.english: ROI 左上角行坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  sx:                 input_control;
  description.english: ROI 左上角列坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  ew:                 input_control;
  description.english: ROI 宽度（= 小图宽度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  eh:                 input_control;
  description.english: ROI 高度（= 小图高度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;


sub_A_roi<-Csub_A_roi[smallimage,bigimage::sy,sx,ew,eh:]
short.german
  Subtract big image from ROI (small - B).;
  
short.english
  ROI 减法（A 系）：大图 ROI 减小图（(A∩Roi) − B），结果写回大图。仅支持 uint2。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  smallimage:         input_object;
  description.english: 小图（uint2，尺寸必须等于 ROI 的 ew×eh）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  bigimage:           input_object;
  description.english: 大图（uint2，结果原地写回）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  sy:                 input_control;
  description.english: ROI 左上角行坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  sx:                 input_control;
  description.english: ROI 左上角列坐标。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  ew:                 input_control;
  description.english: ROI 宽度（= 小图宽度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  eh:                 input_control;
  description.english: ROI 高度（= 小图高度）。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;


CLAHE_image<-CCLAHE_image[inimage:outimage:k_width,k_height,clipLimit:]
short.german
  CLAHE image enhancement.;
  
short.english
  对比度受限自适应直方图均衡，对应 cv::CLAHE。支持 byte/uint2 单通道；TileGridSize 为网格行列数，ClipLimit 为对比度限制阈值。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  inimage:           input_object;
  description.english: 单通道 byte / uint2;
  sem_type:           image;
  multivalue:         optional;

parameter
  outimage:           output_object;
  description.english: 与输入同类型同尺寸;
  sem_type:           image;
  multivalue:         optional;

parameter
  k_width:            input_control;
  description.english: 分块宽度;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  k_height:           input_control;
  description.english: 分块高度;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

parameter
  clipLimit:          input_control;
  description.english: 对比度裁剪阈值;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;

write_image_exif<- CWriteImageExif[::image_path,latitude,longitude,altitude,aperture,shutter_speed,iso_number,focal_length,date_time,camera_make,camera_model:]
short.german
  Write EXIF metadata to image file.;
  
short.english
  给已有图像文件写入 EXIF 元数据（GPS 经纬度/海拔、光圈、快门、ISO、焦距、时间、厂商/型号），基于 exiv2。文件须先存在；字段以多值控制参数传入。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, EXIF, Metadata, GPS;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image_path:         input_control;
  description.english: 图像文件路径（须已存在）。;
  default_type:       string;
  multivalue:         false;
  sem_type:           filename;
  type_list:          string;

parameter
  latitude:           input_control;
  description.english: 纬度;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      0.0;

parameter
  longitude:          input_control;
  description.english: 经度;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      0.0;

parameter
  altitude:           input_control;
  description.english: 海拔（米）;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      0.0;

parameter
  aperture:           input_control;
  description.english: 光圈值;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      0.0;

parameter
  shutter_speed:      input_control;
  description.english: 快门速度（秒）。;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      0.0;

parameter
  iso_number:         input_control;
  description.english: ISO 感光度。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;
  default_value:      100;

parameter
  focal_length:       input_control;
  description.english: 焦距（mm）。;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      0.0;

parameter
  date_time:          input_control;
  description.english: 拍摄时间（格式 YYYY:MM:DD HH:MM:SS）。;
  default_type:       string;
  multivalue:         false;
  sem_type:           string;
  type_list:          string;
  default_value:      "";

parameter
  camera_make:        input_control;
  description.english: 相机厂商字符串。;
  default_type:       string;
  multivalue:         false;
  sem_type:           string;
  type_list:          string;
  default_value:      "Camera";

parameter
  camera_model:       input_control;
  description.english: 相机型号字符串。;
  default_type:       string;
  multivalue:         false;
  sem_type:           string;
  type_list:          string;
  default_value:      "Model";

cv_orb_detect<- Ccv_orb_detect[::DictHandle:]
short.german
  ORB feature detection and descriptor computation using OpenCV.;

short.english
  ORB 特征检测与描述子计算，对应 cv::ORB。输入为 8 位单通道图像（以 dict 对象 InputImage 传入）；输出关键点坐标/数量与描述子（byte，32×N）写回同一 dict。检测参数经 dict 键全开放（NFeatures/ScaleFactor/NLevels/EdgeThreshold/FirstLevel/WTA_K/ScoreType/PatchSize/FastThreshold），未设键取 OpenCV 默认。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, ORB, Feature, Detection, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  DictHandle:         input_control;
  description.english: dict 句柄：设置项以键值传入，未设取默认；结果写回同一 dict。;
  default_type:       handle;
  multivalue:         false;
  sem_type:           handle;
  type_list:          handle;


cv_akaze_detect<- Ccv_akaze_detect[::DictHandle:]
short.german
  AKAZE feature detection and descriptor computation using OpenCV.;

short.english
  AKAZE 特征检测与描述子计算，对应 cv::AKAZE。输入 8 位单通道图像，输出关键点与描述子写回 dict。参数经 dict 键全开放（DescriptorType/Threshold/NOctaves/NOctaveLayers/Diffusivity 等），未设取默认。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, AKAZE, Feature, Detection, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  DictHandle:         input_control;
  description.english: dict 句柄：设置项以键值传入，未设取默认；结果写回同一 dict。;
  default_type:       handle;
  multivalue:         false;
  sem_type:           handle;
  type_list:          handle;


cv_sift_detect<- Ccv_sift_detect[::DictHandle:]
short.german
  SIFT feature detection and descriptor computation using OpenCV.;

short.english
  SIFT 特征检测与描述子计算，对应 cv::SIFT。输入 8 位单通道图像，输出 128 维浮点描述子。参数经 dict 键全开放（NFeatures/NOctaveLayers/ContrastThreshold/EdgeThreshold/Sigma）。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, SIFT, Feature, Detection, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  DictHandle:         input_control;
  description.english: dict 句柄：设置项以键值传入，未设取默认；结果写回同一 dict。;
  default_type:       handle;
  multivalue:         false;
  sem_type:           handle;
  type_list:          handle;


cv_bf_knn_match<- Ccv_bf_knn_match[::DictHandle:]
short.german
  BF KNN feature matching with Lowe's ratio test using OpenCV.;

short.english
  BF 暴力匹配，对应 cv::BFMatcher。参考/目标描述子以 dict 图像传入，输出匹配索引与良好匹配数写回 dict。CrossCheck=1 时走 bf.match()（互为最佳匹配），=0 时走 knnMatch+Lowe ratio test（K≥2）。NormType 默认 NORM_HAMMING，适配 ORB/AKAZE 二进制描述子。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, BF, KNN, Match, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  DictHandle:         input_control;
  description.english: dict 句柄：设置项以键值传入，未设取默认；结果写回同一 dict。;
  default_type:       handle;
  multivalue:         false;
  sem_type:           handle;
  type_list:          handle;


cv_estimate_affine_partial2d<- Ccv_estimate_affine_partial2d[::DictHandle:]
short.german
  Estimate partial affine transformation using RANSAC (OpenCV).;

short.english
  部分仿射变换估计（4 自由度：平移+旋转+各向同性缩放），对应 cv::estimateAffinePartial2D。源/目标点集经 dict 元组传入（各 ≥3 点），结果 HomMat2D（2×3 展开）/Success/InlierCount 及平移/角度/缩放写回 dict；Method/MaxIters/Confidence/RefineIters 经 dict 键可调。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Affine, RANSAC, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  DictHandle:         input_control;
  description.english: dict 句柄：设置项以键值传入，未设取默认；结果写回同一 dict。;
  default_type:       handle;
  multivalue:         false;
  sem_type:           handle;
  type_list:          handle;

cv_estimate_rigid_2d<- Ccv_estimate_rigid_2d[::DictHandle:]
short.german
  Estimate rigid transformation (rotation + translation) using RANSAC (OpenCV).;

short.english
  刚体变换估计（平移+旋转，无缩放），自实现 RANSAC：每轮采样 2 点对闭式解 cos/sin+平移，统计内点取最优。点集经 dict 传入，MaxIter/Seed 可调；结果 HomMat2D/角度/平移写回 dict，Scale 恒为 1.0。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Rigid, Rotation, Translation, RANSAC, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  DictHandle:         input_control;
  description.english: dict 句柄：设置项以键值传入，未设取默认；结果写回同一 dict。;
  default_type:       handle;
  multivalue:         false;
  sem_type:           handle;
  type_list:          handle;

cv_write_image<- Ccv_write_image[inimage::filename,compression:]
short.german
  Save image as PNG file.;
  
short.english
  写图像文件，对应 cv::imwrite。支持 byte/uint2 单通道按扩展名编码（png/jpg/bmp 等）；写失败返回错误码。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, PNG, Write, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  inimage:           input_object;
  description.english: 8/16 位，单/三通道;
  sem_type:           image;
  multivalue:         optional;

parameter
  filename:          input_control;
  description.english: 输出文件路径;
  default_type:      string;
  multivalue:        false;
  sem_type:          string;
  type_list:         string;

parameter
  compression:       input_control;
  description.english: PNG 压缩级别，默认 3;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

cv_mat_mul<- Ccv_mat_mul[imageA,imageB:outimage:alpha,beta,flags:]

short.german
  Matrix multiplication of two 16-bit single-channel images.;

short.english
  两个 16 位单通道图像做矩阵乘法，对应 cv::gemm：C = alpha*A*B (+ beta)。A 为 m×k、B 为 k×n，结果 C 为 m×n；内部转 double 计算后饱和截断回 16 位。Flags 支持字符串枚举（none/transpose_a/transpose_b/transpose_c）或整数 0/1/2/4。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Matrix, Multiply, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  imageA:            input_object;
  description.english: 16 位单通道（m×k）;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageB:            input_object;
  description.english: 16 位单通道（k×n）;
  sem_type:           image;
  multivalue:         optional;

parameter
  outimage:           output_object;
  description.english: 16 位单通道（m×n）;
  sem_type:           image;
  multivalue:         optional;

parameter
  alpha:             input_control;
  description.english: 乘积系数，默认 1.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real,integer;
  default_value:     1.0;

parameter
  beta:              input_control;
  description.english: 叠加项系数，默认 0.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real,integer;
  default_value:     0.0;

parameter
  flags:             input_control;
  description.english: 转置标志（'none'/'transpose_a'/'transpose_b'/'transpose_c' 或 0/1/2/4）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         string,integer;
  default_value:     0;
  value_list:        0,1,2,4;

cv_median_blur<- Ccv_median_blur[image:imageOut:ksize:]
short.german
  Median blur filter for images using OpenCV.;

short.english
  中值滤波，对应 cv::medianBlur。支持 byte/uint2/real 单通道；16 位与浮点图仅支持 Ksize=3 或 5。Ksize 为 ≥1 的奇数核尺寸。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Median, Blur, Filter, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 单通道 byte / uint2 / real;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 与输入同类型同尺寸;
  sem_type:           image;
  multivalue:         optional;

parameter
  ksize:             input_control;
  description.english: 滤波核尺寸，≥3 的奇数，默认 3;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

cv_reshape<- Ccv_reshape[image:imageOut:rows:]
short.german
  Reshape an image matrix using OpenCV (single channel).;

short.english
  改变图像尺寸（最近邻重采样），对应 cv::resize。输出宽为 Rows 参数（历史命名），高度按比例缩放。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Reshape, Matrix, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 单通道 byte / uint2 / real;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 与输入同类型，新尺寸;
  sem_type:           image;
  multivalue:         optional;

parameter
  rows:              input_control;
  description.english: 输出图像新高度；0 表示保持原高度;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     0;

cv_filter2d<- Ccv_filter2d[image,kernel:imageOut:ddepth,anchorX,anchorY,delta,borderType:]
short.german
  Convolve an image with a kernel using OpenCV.;

short.english
  图像卷积，对应 cv::filter2D。卷积核经 real 单通道图像传入；Ddepth 支持字符串枚举（same/s16/f32/f64）或整数（-1=保持原深度）；Anchor 指定锚点（-1=核中心）；BorderType 支持字符串枚举或整数。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Filter, Convolution, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 单通道 byte / uint2 / real;
  sem_type:           image;
  multivalue:         optional;

parameter
  kernel:            input_object;
  description.english: 卷积核，必须为 real 单通道，任意宽高;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 与输入同类型同尺寸;
  sem_type:           image;
  multivalue:         optional;

parameter
  ddepth:            input_control;
  description.english: 输出深度（'same'=-1 / 's16' / 'f32' / 'f64' 或整数）;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:         string,integer;
  default_value:      -1;

parameter
  anchorX:           input_control;
  description.english: 锚点 X（-1=核中心）;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;
  default_value:      -1;

parameter
  anchorY:           input_control;
  description.english: 锚点 Y（-1=核中心）;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:          integer;
  default_value:      -1;

parameter
  delta:             input_control;
  description.english: 结果叠加偏移量;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      0.0;

parameter
  borderType:        input_control;
  description.english: 边界模式（字符串枚举或整数）;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:         string,integer;
  default_value:      4;

cv_measure_pos<- Ccv_measure_pos[image::Column,Row,Phi,Length1,Length2,Sigma,Threshold,Transition:RowEdge,ColumnEdge,Amplitude]
short.german
  Misst gerade Kanten mit einem eindimensionalen Gaußfilter (OpenCV).;

short.english
  一维边缘测量，复刻 HALCON measure_pos（算法本体在 cvr 库 cvr_measure_pos，7 步管线：剖面提取→高斯一阶导核→卷积→局部极大值+阈值→抛物线亚像素→排序→去重/方向过滤/映射）。沿测量矩形主轴扫描，输出边缘行/列坐标与振幅。注意：暗→亮边缘振幅为负（transition=-1 选中），亮→暗为正。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Measure, Edge, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 单通道 byte / uint2 / real（内部转 byte）;
  sem_type:           image;
  multivalue:         optional;

parameter
  Column:            input_control;
  description.english: 测量矩形中心列坐标 x;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     0.0;

parameter
  Row:               input_control;
  description.english: 测量矩形中心行坐标 y;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     0.0;

parameter
  Phi:               input_control;
  description.english: 主轴角度（弧度，逆时针为正）;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     0.0;

parameter
  Length1:           input_control;
  description.english: 沿主轴半长;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     10.0;

parameter
  Length2:           input_control;
  description.english: 垂直主轴半宽;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     5.0;

parameter
  Sigma:             input_control;
  description.english: 高斯平滑标准差;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     1.0;

parameter
  Threshold:         input_control;
  description.english: 振幅阈值;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     5.0;

parameter
  Transition:        input_control;
  description.english: 0=全部，1=暗→亮，-1=亮→暗;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     0;

parameter
  RowEdge:           output_control;
  description.english: 边缘行坐标;
  default_type:      real;
  multivalue:        true;
  sem_type:          number;
  type_list:         real;

parameter
  ColumnEdge:        output_control;
  description.english: 边缘列坐标;
  default_type:      real;
  multivalue:        true;
  sem_type:          number;
  type_list:         real;

parameter
  Amplitude:         output_control;
  description.english: 边缘振幅;
  default_type:      real;
  multivalue:        true;
  sem_type:          number;
  type_list:         real;

cv_blur<- Ccv_blur[image:imageOut:kwidth,kheight,anchorX,anchorY,borderType:]
short.german
  Smooth an image using a normalized box filter (OpenCV).;

short.english
  对图像做归一化盒式滤波（均值滤波），对应 cv::blur；输出与输入同类型同尺寸的单通道图像，支持 byte / uint2 / real。AnchorX/AnchorY 指定核锚点（-1=核中心）；BorderType 为边界模式，支持字符串枚举（如 'border_default'，大小写不敏感）或整数；核宽/高须为 ≥1 的奇数。;



module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Blur, Smooth, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;


parameter
  image:             input_object;
  description.english: 输入单通道图像（byte/uint2/real）。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  kwidth:            input_control;
  description.english: 滤波核宽度，≥1 的奇数，默认 3。;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  kheight:           input_control;
  description.english: 滤波核高度，≥1 的奇数，默认 3。;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  anchorX:           input_control;
  description.english: 锚点 X（-1=核中心），默认 -1。;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     -1;

parameter
  anchorY:           input_control;
  description.english: 锚点 Y（-1=核中心），默认 -1。;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     -1;

parameter
  borderType:        input_control;
  description.english: 边界模式，支持字符串枚举（如 border_default）或整数，默认 4。;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         string,integer;
  default_value:     4;

cv_subtract<- Ccv_subtract[imageA,imageB:imageOut::]
short.german
  Compute the per-element difference of two images (OpenCV).;

short.english
  两图相减（饱和截断），对应 cv::subtract。支持 byte/uint2/real 单通道，两图类型尺寸须一致。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Subtract, Arithmetic, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  imageA:            input_object;
  description.english: 单通道 byte / uint2 / real;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageB:            input_object;
  description.english: 与 ImageA 同类型同尺寸;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 与输入同类型同尺寸;
  sem_type:           image;
  multivalue:         optional;

cv_add_weighted<- Ccv_add_weighted[imageA,imageB:imageOut:alpha,beta,gamma:]
short.german
  Computes the weighted sum of two images (OpenCV).;

short.english
  两图加权相加：Out = alpha*Src1 + beta*Src2 + gamma，对应 cv::addWeighted。支持 byte/uint2/real 单通道。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, AddWeighted, Arithmetic, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  imageA:            input_object;
  description.english: 单通道 byte / uint2 / real;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageB:            input_object;
  description.english: 与 ImageA 同类型同尺寸;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 与输入同类型同尺寸;
  sem_type:           image;
  multivalue:         optional;

parameter
  alpha:             input_control;
  description.english: 权重，默认 1.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     1.0;

parameter
  beta:              input_control;
  description.english: 权重，默认 1.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     1.0;

parameter
  gamma:             input_control;
  description.english: 偏置，默认 0.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     0.0;

cv_solve<- Ccv_solve[imageA,imageB:imageX:methodId:]
short.german
  Solve linear equations using OpenCV.;

short.english
  线性方程组求解 Ax=b，对应 cv::solve。A、B 为 real 单通道图像（A 为 n×n 方阵，B 为 n×m 右端），输出 X（n×m real）；flags 对应 DECOMP_* 方法。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Solve, Linear, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  imageA:            input_object;
  description.english: real 单通道矩阵 A（m×n）;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageB:            input_object;
  description.english: real 单通道矩阵 b（m×1 或 m×k）;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageX:            output_object;
  description.english: 解 x（n×1 或 n×k，real）;
  sem_type:           image;
  multivalue:         optional;

parameter
  methodId:          input_control;
  description.english: 求解方法，默认 0;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     0;

cv_estimate_affine_2d<- Ccv_estimate_affine_2d[::methodId,ransacThreshold,srcRow,srcCol,dstRow,dstCol,maxIters,confidence,refineIters:homMat2D,success,inlierCount]
short.german
  Estimate full affine transformation using OpenCV.;

short.english
  完整仿射变换估计（6 参数），对应 cv::estimateAffine2D。源/目标点集为等长实数元组（≥3 点）；MethodId（RANSAC=8 默认/LMEDS=4）、MaxIters/Confidence/RefineIters 可调；输出 6 元素矩阵 [R00,R10,T0,R01,R11,T1]，顺序同 HALCON hom_mat2d。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Affine, RANSAC, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  methodId:          input_control;
  description.english: RANSAC=8（默认）/ LMEDS=4;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     8;

parameter
  ransacThreshold:   input_control;
  description.english: 重投影阈值，默认 3.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     3.0;

parameter
  srcRow:            input_control;
  description.english: 源点行 / 列坐标;
  default_type:      real;
  multivalue:        true;
  sem_type:          number;
  type_list:         real;

parameter
  srcCol:            input_control;
  description.english: 源点行 / 列坐标;
  default_type:      real;
  multivalue:        true;
  sem_type:          number;
  type_list:         real;

parameter
  dstRow:            input_control;
  description.english: 目标点行 / 列坐标;
  default_type:      real;
  multivalue:        true;
  sem_type:          number;
  type_list:         real;

parameter
  dstCol:            input_control;
  description.english: 目标点行 / 列坐标;
  default_type:      real;
  multivalue:        true;
  sem_type:          number;
  type_list:         real;

parameter
  maxIters:          input_control;
  description.english: RANSAC 最大迭代次数，默认 2000;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     2000;

parameter
  confidence:        input_control;
  description.english: 置信度，默认 0.99;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     0.99;

parameter
  refineIters:       input_control;
  description.english: 最小二乘精炼迭代次数，默认 10;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     10;

parameter
  homMat2D:          output_control;
  description.english: 6 元素仿射矩阵 [R00,R10,T0,R01,R11,T1];
  default_type:      real;
  multivalue:        true;
  sem_type:          number;
  type_list:         real;

parameter
  success:           output_control;
  description.english: 1 = 成功，0 = 失败;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;

parameter
  inlierCount:       output_control;
  description.english: 内点数量;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;

cv_threshold_triangle<- Ccv_threshold_triangle[image:imageOut::threshValue]
short.german
  Triangle automatic thresholding using OpenCV.;

short.english
  三角法自动阈值分割（THRESH_TRIANGLE），仅支持 8 位单通道。输出二值图与自动阈值。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Threshold, Triangle, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: **仅 8 位单通道**（OpenCV 限制）;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 二值图（byte，0 / 255）;
  sem_type:           image;
  multivalue:         optional;

parameter
  threshValue:       output_control;
  description.english: 自动计算出的阈值;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;


cv_threshold<- Ccv_threshold[image:imageOut:thresh,maxval,type:threshUsed]
short.german
  Schwellenwertbildung mit OpenCV.;

short.english
  固定/自动阈值分割，对应 cv::threshold。Type 支持字符串枚举或整数：基础类型 binary/binary_inv/trunc/tozero/tozero_inv，可与 otsu/triangle 用 | 组合（如 binary|otsu）。支持 byte/uint2/real；OTSU/TRIANGLE 仅 byte。输出实际使用阈值 ThreshUsed。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Threshold, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  thresh:            input_control;
  description.english: 阈值（OTSU/TRIANGLE 时作初值，实际值见 ThreshUsed）。;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      128.0;

parameter
  maxval:            input_control;
  description.english: 最大值（二值化输出值）。;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real,integer;
  default_value:      255.0;

parameter
  type:              input_control;
  description.english: 阈值类型：字符串（binary/binary_inv/trunc/tozero/tozero_inv，可与 otsu/triangle 用 | 组合）或整数。;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:         string,integer;
  default_value:      0;
  value_list:         0,1,2,3,4,7,8,16;

parameter
  threshUsed:        output_control;
  description.english: 实际使用的阈值（OTSU/TRIANGLE 时为自动值）。;
  default_type:       real;
  multivalue:         false;
  sem_type:           number;
  type_list:          real;


cv_calc_hist<- Ccv_calc_hist[image:histogram:histSize,rangeMin,rangeMax:]
short.german
  Compute gray-level histogram using OpenCV.;

short.english
  灰度直方图，对应 cv::calcHist。支持 byte/uint2/real 单通道；输出 1×HistSize 的 real 直方图，范围 [RangeMin, RangeMax)。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Histogram, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 单通道 byte / uint2 / real;
  sem_type:           image;
  multivalue:         optional;

parameter
  histogram:         output_object;
  description.english: real，1×HistSize（各桶计数）;
  sem_type:           image;
  multivalue:         optional;

parameter
  histSize:          input_control;
  description.english: 直方图桶数，默认 256;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     256;

parameter
  rangeMin:          input_control;
  description.english: 像素值范围，默认 0.0 / 256.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     0.0;

parameter
  rangeMax:          input_control;
  description.english: 像素值范围，默认 0.0 / 256.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     256.0;

cv_match_template<- Ccv_match_template[image,templateImage:result:methodId:]
short.german
  Template matching using OpenCV (TM_CCOEFF_NORMED).;

short.english
  模板匹配，对应 cv::matchTemplate。支持 byte/real（OpenCV 限制不支持 16 位）；MethodId 支持字符串枚举（sqdiff/sqdiff_normed/ccorr/ccorr_normed/ccoeff/ccoeff_normed）或 0-5，默认 ccoeff_normed。输出 real 得分图，尺寸 (W−w+1)×(H−h+1)。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Template, Match, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 单通道 byte / real（OpenCV 限制，不支持 16 位）;
  sem_type:           image;
  multivalue:         optional;

parameter
  templateImage:     input_object;
  description.english: 与 Image 同类型，尺寸 ≤ Image;
  sem_type:           image;
  multivalue:         optional;

parameter
  result:            output_object;
  description.english: real 得分图，尺寸 (W−w+1)×(H−h+1);
  sem_type:           image;
  multivalue:         optional;

parameter
  methodId:          input_control;
  description.english: 匹配方法（'sqdiff'/'sqdiff_normed'/'ccorr'/'ccorr_normed'/'ccoeff'/'ccoeff_normed' 或 0-5，默认 5）;
  default_type:       integer;
  multivalue:         false;
  sem_type:           number;
  type_list:         string,integer;
  default_value:      5;
  value_list:         0,1,2,3,4,5;

cv_kmeans<- Ccv_kmeans[samples:labels,centers:K,attempts,termEps,termMaxIter,flags:]
short.german
  K-means clustering using OpenCV.;

short.english
  K 均值聚类，对应 cv::kmeans。样本为 real N×D 图像（N 样本每行 D 维）；输出 Labels（N×1）与 Centers（K×D）。Flags 支持字符串枚举（random/pp/use_initial_labels）或整数，默认 pp。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, KMeans, Cluster, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  samples:           input_object;
  description.english: real N×D（N 个样本，每行 D 维特征）;
  sem_type:           image;
  multivalue:         optional;

parameter
  labels:            output_object;
  description.english: real N×1（整数标签，以浮点承载）;
  sem_type:           image;
  multivalue:         optional;

parameter
  centers:           output_object;
  description.english: real K×D 聚类中心;
  sem_type:           image;
  multivalue:         optional;

parameter
  K:                 input_control;
  description.english: 聚类数，默认 2;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     2;

parameter
  attempts:          input_control;
  description.english: 重复尝试次数，默认 10;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     10;

parameter
  termEps:           input_control;
  description.english: 终止精度，默认 1.0e-4;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     1.0e-4;

parameter
  termMaxIter:       input_control;
  description.english: 最大迭代次数，默认 100;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     100;

parameter
  flags:             input_control;
  description.english: 中心初始化（'random'/'pp'/'use_initial_labels' 或 0/1/2，默认 1）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         string,integer;
  default_value:     1;
  value_list:        0,1,2;

cv_multi_frame_median<- Ccv_multi_frame_median[images:imageMedian::]
short.german
  Berechnet den pixelweisen Median über mehrere Real-Bilder (OpenCV).;

short.english
  多帧中值滤波：对 N 张同尺寸 real 图像逐像素取中值，输出单张 real 中值图（N=3 有特化快路径）。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Median, MultiFrame, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  images:            input_object;
  description.english: N 张 real 单通道图，尺寸一致，N ≥ 2;
  sem_type:           image;
  multivalue:         true;

parameter
  imageMedian:       output_object;
  description.english: real 单通道中值图;
  sem_type:           image;
  multivalue:         optional;

cv_sobel<- Ccv_sobel[image:imageOut:Dx,Dy,Ksize,Ddepth,Scale,Delta,BorderType:]
short.german
  Kantendetektion mit dem Sobel-Operator (OpenCV).;

short.english
  Sobel 一阶导数边缘检测，对应 cv::Sobel。输入 byte/uint2/real，输出 real 梯度；Dx/Dy 为求导阶数（0/1，和≥1），Ksize 为核尺寸（1/3/5/7），Ddepth 支持字符串枚举（same/s16/f32/f64），BorderType 支持字符串枚举或整数。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Sobel, Edge, Gradient, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 单通道 byte / uint2 / real;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:          output_object;
  description.english: 统一输出 real（CV_32F，梯度含符号）;
  sem_type:           image;
  multivalue:         optional;

parameter
  Dx:                input_control;
  description.english: x 方向导数阶数（0~2），默认 1;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     1;

parameter
  Dy:                input_control;
  description.english: y 方向导数阶数（0~2），默认 0;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     0;

parameter
  Ksize:             input_control;
  description.english: 核尺寸（1 / 3 / 5 / 7），默认 3;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  Ddepth:            input_control;
  description.english: 输出深度（-1 / 3=CV_16S / 5=CV_32F / 6=CV_64F），默认 6;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         string,integer;
  default_value:     6;

parameter
  Scale:             input_control;
  description.english: 缩放系数，默认 1.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     1.0;

parameter
  Delta:             input_control;
  description.english: 偏置，默认 0.0;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real;
  default_value:     0.0;

parameter
  BorderType:        input_control;
  description.english: OpenCV 边界模式（1=REPLICATE 等），默认 1;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         string,integer;
  default_value:     1;

cv_magnitude<- Ccv_magnitude[X,Y:Magnitude::]
short.german
  Berechnet die Gradienten-Magnitude sqrt(X^2+Y^2) (OpenCV).;

short.english
  二维向量模长 sqrt(x²+y²)，对应 cv::magnitude。输入两图同类型同尺寸（byte/uint2/real），内部转 32F 计算，输出 real。;

module
  foundation;

chapter.german
  BenutzerErweiterungen;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Magnitude, Gradient, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  X:                 input_object;
  description.english: 单通道 byte / uint2 / real;
  sem_type:           image;
  multivalue:         optional;

parameter
  Y:                 input_object;
  description.english: 与 X 同类型同尺寸;
  sem_type:           image;
  multivalue:         optional;

parameter
  Magnitude:         output_object;
  description.english: 统一输出 real（CV_32F）;
  sem_type:           image;
  multivalue:         optional;

cv_morphology_ex<- Ccv_morphology_ex[image:imageOut:op,shape,kwidth,kheight,iterations:]

short.english
  通用灰度形态学，对应 cv::morphologyEx。Op/Shape 支持字符串枚举（erode/dilate/open/close/gradient/tophat/blackhat × rect/cross/ellipse）或整数；Iterations≥1 循环实现。支持 byte/uint2/real。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  op:                input_control;
  description.english: 形态学操作（erode/dilate/open/close/gradient/tophat/blackhat，也可整数 0-6）;
  default_type:      string;
  multivalue:        false;
  sem_type:          string;
  type_list:         string,integer;
  default_value:     "open";

parameter
  shape:             input_control;
  description.english: 结构元形状（rect/cross/ellipse，也可整数 0-2）;
  default_type:      string;
  multivalue:        false;
  sem_type:          string;
  type_list:         string,integer;
  default_value:     "rect";

parameter
  kwidth:            input_control;
  description.english: 核宽度，≥1;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  kheight:           input_control;
  description.english: 核高度，≥1;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  iterations:        input_control;
  description.english: 迭代次数 ≥1（morphologyEx 循环实现）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     1;

cv_gray_erosion_rect<- Ccv_gray_erosion_rect[image:imageOut:width,height:]

short.english
  灰度腐蚀（矩形核），对应 cv::morphologyEx MORPH_ERODE。核尺寸 Width×Height；支持 byte/uint2/real，输出同类型。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, Erosion, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  width:             input_control;
  description.english: 矩形核宽度（MaskWidth）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  height:            input_control;
  description.english: 矩形核高度（MaskHeight）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

cv_gray_dilation_rect<- Ccv_gray_dilation_rect[image:imageOut:width,height:]

short.english
  灰度膨胀（矩形核），对应 cv::morphologyEx MORPH_DILATE。核尺寸 Width×Height；支持 byte/uint2/real。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, Dilation, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  width:             input_control;
  description.english: 矩形核宽度（MaskWidth）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  height:            input_control;
  description.english: 矩形核高度（MaskHeight）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

cv_gray_opening_rect<- Ccv_gray_opening_rect[image:imageOut:width,height:]

short.english
  灰度开运算（矩形核）：先腐蚀后膨胀，去亮噪点。对应 cv::morphologyEx MORPH_OPEN。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, Opening, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  width:             input_control;
  description.english: 矩形核宽度（MaskWidth）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  height:            input_control;
  description.english: 矩形核高度（MaskHeight）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

cv_gray_closing_rect<- Ccv_gray_closing_rect[image:imageOut:width,height:]

short.english
  灰度闭运算（矩形核）：先膨胀后腐蚀，填暗孔洞。对应 cv::morphologyEx MORPH_CLOSE。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, Closing, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  width:             input_control;
  description.english: 矩形核宽度（MaskWidth）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

parameter
  height:            input_control;
  description.english: 矩形核高度（MaskHeight）;
  default_type:      integer;
  multivalue:        false;
  sem_type:          number;
  type_list:         integer;
  default_value:     3;

cv_gray_erosion_circle<- Ccv_gray_erosion_circle[image:imageOut:radius:]

short.english
  灰度腐蚀（圆核）：核为 2R+1 椭圆核。对应 cv::morphologyEx MORPH_ERODE。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, Erosion, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  radius:            input_control;
  description.english: 圆半径（核尺寸 = 2R+1 椭圆核）;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real,integer;
  default_value:     3.0;

cv_gray_dilation_circle<- Ccv_gray_dilation_circle[image:imageOut:radius:]

short.english
  灰度膨胀（圆核）：核为 2R+1 椭圆核。对应 cv::morphologyEx MORPH_DILATE。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, Dilation, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  radius:            input_control;
  description.english: 圆半径（核尺寸 = 2R+1 椭圆核）;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real,integer;
  default_value:     3.0;

cv_gray_opening_circle<- Ccv_gray_opening_circle[image:imageOut:radius:]

short.english
  灰度开运算（圆核）。对应 cv::morphologyEx MORPH_OPEN，核尺寸 2R+1。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, Opening, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  radius:            input_control;
  description.english: 圆半径（核尺寸 = 2R+1 椭圆核）;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real,integer;
  default_value:     3.0;

cv_gray_closing_circle<- Ccv_gray_closing_circle[image:imageOut:radius:]

short.english
  灰度闭运算（圆核）。对应 cv::morphologyEx MORPH_CLOSE，核尺寸 2R+1。;

module
  foundation;

chapter.english
  UserExtensions;

keywords.english
  UserExtensions, Morphology, Grayscale, Closing, OpenCV;

parallelization
  process_exclusively: false;
  process_locally:     false;
  process_mutual:      false;
  method:              none;

parameter
  image:             input_object;
  description.english: 输入单通道图像。;
  sem_type:           image;
  multivalue:         optional;

parameter
  imageOut:           output_object;
  description.english: 输出图像，类型尺寸与输入一致。;
  sem_type:           image;
  multivalue:         optional;

parameter
  radius:            input_control;
  description.english: 圆半径（核尺寸 = 2R+1 椭圆核）;
  default_type:      real;
  multivalue:        false;
  sem_type:          number;
  type_list:         real,integer;
  default_value:     3.0;
